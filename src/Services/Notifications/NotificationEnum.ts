export enum NotificationRequestType {
    Get,
    ReadCurrent,
    ReadAll,
    Delete
}

export enum NotificationEndpoints {
    GetData = "/GetData",
    ReadCurrent  = "/ReadCurrent",
    ReadAll = "/ReadAll",
    Delete = "/Delete"
}
